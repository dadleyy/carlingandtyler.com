.registries.view
  ul.registry-list
    li.registry(ng-repeat="registry in registries")
      .background-image(style="background-image: url({{registry.background}})")
      .registry-text
        a.script.display.bold.registry-link(href="{{registry.url}}" target="_blank")
          span.highlight {{registry.name}}
